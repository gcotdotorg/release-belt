services:
  releasebelt:
    image: 705098187465.dkr.ecr.us-east-2.amazonaws.com/packages:latest
    container_name: gcot-packages
    volumes:
      - ./.env:/var/www/html/.env
    networks:
      - npm_proxy

networks:
  npm_proxy:
    external: true
