services:
  releasebelt:
    image: 705098187465.dkr.ecr.us-east-2.amazonaws.com/packages:latest
    volumes:
      - ./.env:/var/www/html/.env
    networks:
      - npm_proxy

networks:
  npm_proxy:
    external: true
